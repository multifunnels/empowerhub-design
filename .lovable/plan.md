## Fix

Replace the broken `public/tsi-logo.png` with the uploaded TSI logo so it shows on `/sharon-aizen`.

1. Copy `user-uploads://TSI-LOGO.png` → `public/tsi-logo.png` (overwrite).
2. No code changes needed — `Sharon.tsx` already references `/tsi-logo.png`.